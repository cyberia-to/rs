[package]
name = "rs-lang"
version = "0.1.0"
edition = "2021"
description = "Rs standard library — types, bounded collections, fixed-point, arena, channels"

[dependencies]
rs-lang-macros = { path = "../macros", optional = true }

[features]
default = []
std = []
macros = ["rs-lang-macros"]
