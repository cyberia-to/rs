[package]
name = "mir-format"
version = "0.1.0"
edition = "2021"
description = "Serialized MIR format for Rs → Trident pipeline"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
